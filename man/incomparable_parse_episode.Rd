% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/incomparable.R
\name{incomparable_parse_episode}
\alias{incomparable_parse_episode}
\title{Parse a single Incomparable episode page}
\usage{
incomparable_parse_episode(episode_url, cache = TRUE)
}
\arguments{
\item{episode_url}{The per-episode URL,
e.g. \code{"https://www.theincomparable.com/sophomorelit/190/"}.}

\item{cache}{(\code{logical(1)}) Toggle the httr2 HTTP cache. Default \code{TRUE}.}
}
\value{
A one-row tibble with columns \code{summary} and \code{topic} (either
may be \code{NA_character_} if the page doesn't expose them).
}
\description{
Recovers \code{summary} (and \code{topic} when present) for episodes that
aren't on the archive page yet. The archive page is re-rendered on
a slower cadence than \code{stats.txt} updates, so the newest episode of
an active show is typically missing from the archive for hours to
weeks. \code{incomparable_get_episodes()} calls this automatically for
any episode in \code{stats.txt} that the archive doesn't list.
}
\examples{
\dontrun{
incomparable_parse_episode("https://www.theincomparable.com/sophomorelit/190/")
}
}
